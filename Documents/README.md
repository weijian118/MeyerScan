# MeyerScan 重构文档入口

从 2026-07-12 起，本目录是重构项目核心文档的唯一权威位置，并随源码一起提交 GitHub、进入 `F:\MeyerScan-Reposit` 本地历史仓库。

## 四份核心文档

| 文档 | 唯一职责 | 不在此处维护 |
|------|----------|--------------|
| `MeyerScan重构任务总览.md` | 产品范围、总体模块清单、核心原则、全局开发约束 | 具体接口代码、逐次测试流水 |
| `MeyerScan架构设计与接口规范.md` | 模块边界、依赖方向、接口合同、数据流、编码与 UI 规范 | 任务完成百分比、日常对话流水 |
| `MeyerScan重构开发进度跟踪.md` | 模块状态、当前阻塞、已完成验证和下一步 | 重复展开架构论证 |
| `MeyerScan重构-AI协作记录.md` | 按时间记录需求、采纳决策、代码修改、验证结果和遗留项 | 覆盖前三份文档中的现行规范 |

## 维护顺序

1. 后续任务开始前，先读取本目录四份核心文档和目标模块自己的 `README.md` / `CHANGELOG.md`。
2. 方案变化先修改本目录权威文档，再同步到 `D:\wj\重构文档` 的同名镜像；禁止分别编辑后再人工猜测冲突。
3. 模块行为变化同时更新模块内 `README.md` / `CHANGELOG.md`，并以中文详细提交信息提交。
4. 提交前运行注释安全检查、受影响模块测试、MainExe 主链路 smoke 和 `git diff --check`。
5. GitHub 提交完成后，使用 `tools\BackupToLocalRepository.ps1` 整体同步到本地仓库；不得只备份单个模块。

## 辅助规范

- `PowerShell开发与自动化脚本规范.md` 记录 PowerShell 5.1、编码、路径、退出码、删除和本地备份脚本约束。
- 可执行脚本摘要位于 `F:\MeyerScan\tools\README.md`。
- `D:\wj\重构文档\glm52` 和其他评审资料是设计输入，不自动成为现行规范；采纳结果必须写入上述四份核心文档。
