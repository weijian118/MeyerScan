# 登录 SDK 运行依赖

该目录保存登录 SDK 的运行依赖。普通依赖由公共 CMake/props 在构建后复制到
EXE 同级目录。

`MeyerScanNetworkHelper.dll` 不进入 GitHub。当前二进制内嵌云端访问凭据，必须由
受控本地 SDK 或安装包制作环境单独提供。缺少该文件时，源码仍可编译，但登录网络
功能和完整运行 smoke 不可用。

安全整改要求：轮换旧凭据，移除 DLL 内硬编码长期密钥，改用安全配置、系统凭据存储
或服务端签发的短期令牌。整改完成并通过二进制密钥扫描后再决定是否随仓库分发。
